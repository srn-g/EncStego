namespace EncStego.Classes
{
    public static class Global
    {
        public enum DataType { Text = 1 }

        public enum CryptoType { AES = 1 }

        public const int MAX_PART_SIZE_B = 256;
    }
}
